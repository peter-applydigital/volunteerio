// Display a commitment that a user can accept

// TODO: show commitment details
// TODO: allow a user to accept a commitment
// TODO: add the accepted commitment to a user's calendar (can be an ical, gcal event etc.)

const Commitment = () => {
  return <h1>Commitment</h1>;
};

export default Commitment;
